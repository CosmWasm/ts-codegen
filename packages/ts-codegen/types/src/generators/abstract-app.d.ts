import { AbstractAppOptions, ContractInfo } from 'wasm-ast-types';
import { BuilderFile } from '../builder';
declare const _default: (contractName: string, contractInfo: ContractInfo, outPath: string, abstractAppOptions?: AbstractAppOptions) => Promise<BuilderFile[]>;
export default _default;
